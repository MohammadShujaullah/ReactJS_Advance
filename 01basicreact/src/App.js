import Chai from "./Chai";

function App() {
  return (          // we can only return one component at 
                            // atime , if you want to return multiple,
                            //  then you have put it in a dev
    <div>
      <h1>Hello bachchon!!</h1>
      <Chai />
    </div>
  );
}

export default App;
